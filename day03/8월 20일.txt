8월 20일
23부터
--1교시\
이클립스

주석설정

이클립스 자바 탬플릿

shift+alt_j

디버그
한단계식 f6
다음단계로 

오른쪽 마우스 close all-> 모두 닫힘
자기가 디버그 하고 싶은 곳에다 더블클릭해서 점 만들기
마우스 오른쪽 클릭 ->디버그 에즈
f8 누르면 빠져나감
왼쪽 창에서 마우스 오른쪽 버튼 리무브 클릭 ->다 지워짐
나가는법
오른쪽 딱정벌레 오른쪽 클릭후 나가기
자바 클릭
점 더블클릭해서 다시 없애기

for문 if문 공부할 때 좋음

--2교시

수업파일 가지고 가기 

마우ㅡㅅ 오른쪽 export -general-archive file

오른쪽 공간에서 import general-ex(두번째거)-2번째거 선택-파일 가지고 오기

제어문 for
(바로 계산식 넣을 수 있음)

--3교시

다중 for

run configration

행렬 자리 바꾸기

--4교시

행 하나만 잘 가지고 오면 나머지 반복문 돌리면 됨
자바는 행 단위로 읽어옴 
열 단위로 가지고 올려면 배열로 쓰는데 굳이 그럴 이유가 없음
행단위로 읽어와서(클래스)->do, while문을 많이 씀(열단위)
자바에서는 행단위의 데이터 처리이기 때문에 for문을 거의 안씀

while문->데이터 연동햇어 다음에 데이터가 없으면 멈춰 그래서 초기값이 안들어감, 행위반복을 멈춤,수치반복을 많이 사용하지 않기 때문에 for문보다 while문을 더 많이씀


자바를 잘하면 파이썬은 이틀만에 다함

클래스 개념이 뭐지?

--5교시
//28
do whlie 문
맞으면 들어가서 반복되는 것 
do->반복
while->조건

break문 continue 문 

--6교시

continue 문
AA : -> AA: 붙어있는 쪽으로 이동

break 문
AA : ->빠져나가기, 이 조건일 때 그 함수식을 빠져나감

--7교시(32)

브레이크는 상관없는데 continue는 신경써야함 
---제어문 끝
표준 입출력(33)
모니터=표준입출력
소켓=표준입출력
파일=표준입출력
모니터=출력
마우스
뷰=데이터셋

System.in -> 키보드
System.out-> 모니터
close()로 반환

import java.util.Scanner; 
키보드 입력 대 무조건 쓰기

Scanner sc=new Scanner(System.in);
// 키보드를 지칭하는 명령어
sc를 통해서 다 들어온다고 생각하면 됨
외부에서 들어온 거는 다 스트링
문자열로 들어와서 문자열로 출력
정수형으로 받아와서 연산을 할려면?->정수로 바꿔야함
System.out.println("숫자입력:");
		int c=sc.nextInt();
		System.out.println(++c);


--8교시(33,34)
웹도 받아오는게 다 문자열이다
표준 입출력

문자열로 들어오니까 타입을 바꿔야한다
형식이 엄격함
스트링은 엄격히 자료형이 아니고 클래스 
함수를 이용해서 바꿔줄 수 밖에 없다.

--9교시
문제 04 반복문 풀기 16문제

내일 배열
금요일 함수

